#include<stdio.h>
int main()
{
       long long int n,i=0,count=0;
       scanf("%I64d",&n);
       while(n!=0)
       {
              n%10=a[i];
              n=n/10;
              i++;
       }
       while(a[i])
       {
              if(a[i]==7 || a[i]==4)
                   count++;
       }
       if(count)
}
